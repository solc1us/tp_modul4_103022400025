// See https://aka.ms/new-console-template for more information
using TP_MODUL4_103022400025;

Console.WriteLine("Hello, World!");

// Example usage of GetGradeByScore
double score = 75.0;
string grade = GetGradeByScore(score);
Console.WriteLine($"Score: {score}, Grade: {grade}");

// Example usage of KodePos
KodePos kodePos = new KodePos();
string kelurahan = "Batununggal";
int kode = kodePos.getKodePos(kelurahan);
Console.WriteLine($"Kelurahan {kelurahan} memiliki kode pos: {kode}");

static string GetGradeByScore(double studentScore)
{
    string[] grade = { "A", "AB", "B", "BC", "C", "D", "E" };
    double[] rangeLimit = { 80.0, 70.0, 65.0, 60.0, 50.0, 40.0, 0.0 };
    int maxGradeLevel = grade.Length - 1;
    string studentGrade = "E";
    int gradeLevel = 0;
    while ((studentGrade == "E") && (gradeLevel < maxGradeLevel))
    {
        if (studentScore > rangeLimit[gradeLevel])
            studentGrade = grade[gradeLevel];
        gradeLevel = gradeLevel + 1;
    }
    return studentGrade;
}
